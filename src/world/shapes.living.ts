import {
  BoxGeometry, CylinderGeometry, SphereGeometry, TorusGeometry,
  type BufferGeometry,
} from 'three';
import type { ShapeIdLiving } from './generation';
import {
  assemble, hollow, invert, DARK, GLASS, METAL, part, SHINE, WHITE, WRAP, soft,
} from './shapes.kit';
import { TILE } from './atlas';

/** X축으로 돌린 원기둥·토러스 — 축이 Z가 된다 */
const LIE_Z: readonly [number, number, number] = [Math.PI / 2, 0, 0];
/** 눕힌 원기둥. 원기둥 축은 Y라 Z로 90° 돌리면 X축이 된다 */
const LIE_X: readonly [number, number, number] = [0, 0, Math.PI / 2];

/**
 * **거실 소품 — 여섯 종.**
 *
 * ## 왜 이 파일이 생겼나
 *
 * 사용자가 「오브젝트들이 정확히 뭔지 잘 모르겠어」라고 했다. 형상을 세어보니
 * 거실에 나오는 스무 종 중 **절반이 1~3cm** 였다 — 개미(1) · 클립(1) · 압정(3) ·
 * 단추(2) · 동전(2) · 각설탕(3). **5cm 공에게 1cm 물체는 점이다.** 그 크기에서는
 * 부품을 아무리 깎아도 안 읽힌다. 형상 문제가 아니라 **크기 문제**였다.
 *
 * 그리고 더 큰 게 있었다 — **거실 물건이랄 게 없었다.** 책도 잡지도 시계도 액자도
 * 비디오테이프도 없이 사탕·클립·압정이 굴러다니는 방을 거실이라고 부르고 있었다.
 *
 * 그래서 여기 있는 여섯은 전부 **10~25cm**, 거실에서 바로 알아보는 것들이다.
 *
 * ## 인쇄가 부품보다 세다
 *
 * 시계는 문자판이 있어야 시계다. 부품을 열둘로 늘려도 민짜면 원통이고,
 * 원통 하나라도 문자판이 찍혀 있으면 시계다. 그래서 넷은 새 인쇄 칸을 문다
 * (`TILE.BOOK` · `VIDEO` · `CLOCK` · `PICTURE` — 이 작업에서 그렸다).
 *
 * ## 규약
 *
 * 다른 빌더와 같다. `shapes.kit.ts` 만 import 한다(`shapes.ts` 를 물면 순환 참조).
 * 치수는 실제 cm 감각으로 쓰고, `assemble()` 의 `normalize()` 가 최장축을 1.0 으로
 * 맞추며 바닥을 y = −0.5 에 놓는다.
 * **`part()` 의 5번째 인자는 배율이 아니라 인쇄 칸 번호다.**
 */
export const LIVING_BUILDERS: Record<ShapeIdLiving, () => BufferGeometry> = {

  /**
   * 책 (22cm) — **세워둔** 책.
   *
   * 눕히면 그냥 판때기다. 세워야 책등이 보이고, 책등이 보여야 책장에 꽂힌 책이 된다.
   * 종이 쪽(앞면)과 표지(뒷면)의 색·두께가 달라야 「닫힌 책」으로 읽힌다.
   */
  책: () => assemble([
    part(soft(0.62, 1.00, 0.16, 0.1), WHITE, [0, 0.50, 0], undefined, TILE.BOOK),
    // 책배 — 종이 단면. 표지보다 밝고 살짝 안쪽으로 들어간다
    // 책배 — 종이 단면. `PAPER`(대비 0.04)로는 표지와 안 갈린다. 종이는 «희다»
    part(new BoxGeometry(0.58, 0.94, 0.10), WRAP, [0.015, 0.50, 0.045]),
    // 책등. 표지가 접히는 쪽이라 조금 두껍다
    part(soft(0.05, 1.00, 0.17, 0.3), WHITE, [-0.31, 0.50, 0]),
    // 책등 위아래 가름끈
    part(new BoxGeometry(0.06, 0.035, 0.18), [0.85, 0.72, 0.45], [-0.31, 0.94, 0]),
    part(new BoxGeometry(0.06, 0.035, 0.18), [0.85, 0.72, 0.45], [-0.31, 0.06, 0]),
    // 띠지 — 아래쪽 4분의 1을 두른다
    part(new BoxGeometry(0.63, 0.20, 0.175), [0.88, 0.40, 0.24], [0, 0.17, 0]),
    // 갈피 리본
    part(new BoxGeometry(0.02, 0.26, 0.012), [0.78, 0.24, 0.22], [0.24, 0.06, 0.088]),
  ]),

  /**
   * 비디오테이프 (19cm) — VHS 카세트.
   *
   * **릴 창 두 개가 정체다.** 창 없이 검은 상자를 놓으면 벽돌이다.
   * 앞 셔터(은색 띠)와 라벨 종이가 나머지를 맡는다.
   */
  비디오테이프: () => assemble([
    part(soft(1.00, 0.135, 0.57, 0.14), DARK, [0, 0.09, 0]),
    // 위 라벨면. 손글씨가 찍힌다
    // 위 라벨면 — 몸통(검정)과 확실히 갈려야 한다. 넓이도 키운다
    part(new BoxGeometry(0.94, 0.022, 0.53), WRAP, [0, 0.164, 0], undefined, TILE.VIDEO),
    // 릴 창 둘 — 안이 비쳐야 테이프다
    // 릴 창 둘 — 라벨보다 «짙어야» 창으로 읽힌다. 라벨 «안»에 가둔다
    // 릴 창 — 테이프 팔레트가 «검정»(밝기 0.18)이라 0.30 으로는 대비가 0.03 이다.
    // 검은 물건 위의 창은 «밝은» 쪽이어야 보인다
    ...([-0.19, 0.19] as const).map((x) =>
      part(new CylinderGeometry(0.13, 0.13, 0.026, 14), [1.9, 1.95, 2.05], [x, 0.170, 0.06])),
    // 릴 허브
    ...([-0.19, 0.19] as const).map((x) =>
      part(new CylinderGeometry(0.045, 0.045, 0.034, 8), [1.0, 1.02, 1.10], [x, 0.178, 0.06])),
    // 앞 셔터. 은색 띠 하나가 앞뒤를 갈라준다
    // 앞 셔터 — 검정 위의 은색은 `SHINE` 급이라야 띠로 보인다
    part(new BoxGeometry(0.86, 0.10, 0.03), SHINE, [0, 0.09, -0.30]),
    // 뒤쪽 굽 둘
    part(new BoxGeometry(0.10, 0.03, 0.07), DARK, [-0.36, 0.015, 0.22]),
    part(new BoxGeometry(0.10, 0.03, 0.07), DARK, [0.36, 0.015, 0.22]),
  ]),

  /**
   * 탁상시계 (12cm) — 종 둘 달린 자명종.
   *
   * **문자판이 전부다.** 눈금과 바늘이 없으면 발 달린 원통이다.
   * 위의 종 둘과 뒤의 태엽이 「자명종」이라는 실루엣을 만든다.
   */
  탁상시계: () => assemble([
    // 몸통 — 눕힌 원통. 앞면이 문자판이다
    part(new CylinderGeometry(0.40, 0.40, 0.24, 20), WHITE, [0, 0.42, 0], LIE_Z),
    // 문자판. 몸통보다 살짝 앞으로 나와야 테두리가 생긴다
    part(new CylinderGeometry(0.345, 0.345, 0.03, 20), WHITE, [0, 0.42, 0.13], LIE_Z, TILE.CLOCK),
    // 베젤 — 문자판을 두르는 테. 없으면 그림이 몸통에 스티커처럼 붙어 보인다
    part(new TorusGeometry(0.375, 0.035, 4, 20), METAL, [0, 0.42, 0.115], LIE_Z),
    // 종 둘. 자명종의 정체
    part(new SphereGeometry(0.155, 12, 8), METAL, [-0.26, 0.80, 0]),
    part(new SphereGeometry(0.155, 12, 8), METAL, [0.26, 0.80, 0]),
    // 종 자루
    part(new CylinderGeometry(0.028, 0.028, 0.12, 6), METAL, [-0.26, 0.71, 0]),
    part(new CylinderGeometry(0.028, 0.028, 0.12, 6), METAL, [0.26, 0.71, 0]),
    // 종 사이 망치
    part(new CylinderGeometry(0.022, 0.022, 0.30, 6), METAL, [0, 0.78, -0.05], LIE_X),
    // 발 둘. 앞으로 기울어져야 탁상시계로 보인다
    part(new CylinderGeometry(0.035, 0.045, 0.16, 6), METAL, [-0.24, 0.08, 0.10]),
    part(new CylinderGeometry(0.035, 0.045, 0.16, 6), METAL, [0.24, 0.08, 0.10]),
    // 뒤 태엽 손잡이
    part(new CylinderGeometry(0.06, 0.06, 0.05, 6), METAL, [0, 0.42, -0.16], LIE_Z),
  ]),

  /**
   * 액자 (20cm) — 세워둔 사진틀.
   *
   * **테 넷이 안쪽을 감싸야 액자다.** 판때기에 그림만 찍으면 사진이지 액자가 아니다.
   * 뒤 받침대가 「세워져 있다」를 만든다.
   */
  액자: () => assemble([
    // 그림면. 안쪽으로 들어가 있어야 테가 튀어나온 게 보인다
    part(new BoxGeometry(0.72, 0.86, 0.03), WHITE, [0, 0.52, 0], undefined, TILE.PICTURE),
    // 테 넷 — 가로 테를 세로 테보다 짧게. 길이가 같으면 끝면이 같은 평면이다
    // 테 넷 — **그림면과 확실히 갈려야 «테가 튀어나왔다»가 보인다.** 액자 팔레트는
    // 나무·은색 둘이라 `WOOD`(0.40)로는 대비가 0.08 이다. 계수를 더 벌린다
    part(soft(0.78, 0.09, 0.075, 0.3), [0.30, 0.22, 0.15], [0, 0.955, 0.016]),
    part(soft(0.78, 0.09, 0.075, 0.3), [0.30, 0.22, 0.15], [0, 0.085, 0.016]),
    part(soft(0.09, 1.00, 0.07, 0.3), [0.30, 0.22, 0.15], [-0.385, 0.52, 0.015], undefined, TILE.WOOD_C),
    part(soft(0.09, 1.00, 0.07, 0.3), [0.30, 0.22, 0.15], [0.385, 0.52, 0.015], undefined, TILE.WOOD_C),
    /**
     * 뒤판 — **이게 몸통이다.** 상자 겉넓이가 그림면보다 넓어서 자가 여기를 기준으로
     * 삼는다. 짙게 뒀더니 나무 테와 대비가 0.03 이라 헛색 다섯이 잡혔다.
     * 액자 뒤판은 원래 «크라프트 판지»라 밝다 — 그러면 테도 그림면도 갈린다.
     */
    part(new BoxGeometry(0.78, 0.90, 0.02), [0.85, 0.76, 0.62], [0, 0.52, -0.026],
      undefined, TILE.CARDBOARD),
    // 받침대. 뒤로 비스듬히 뻗는다
    part(new BoxGeometry(0.16, 0.62, 0.02), [0.85, 0.76, 0.62], [0, 0.34, -0.14], [0.42, 0, 0]),
  ]),

  /**
   * 귤 (7cm) — 눌린 구.
   *
   * 사과와 헷갈리면 안 된다. **사과는 세로로 길고 귤은 납작하다.**
   * 꼭지가 위로 솟는 사과와 달리 귤은 꼭지 자리가 옴폭 들어간다.
   */
  귤: () => assemble([
    part(new SphereGeometry(0.5, 16, 10).scale(1, 0.76, 1), WHITE, [0, 0.38, 0]),
    // 꼭지 자리 — 옴폭한 자국. 귤과 사과를 가르는 한 끗
    part(new CylinderGeometry(0.09, 0.11, 0.05, 8), [0.72, 0.52, 0.22], [0, 0.755, 0]),
    // 배꼽 — 아래쪽 자국
    part(new CylinderGeometry(0.06, 0.045, 0.04, 6), [0.78, 0.56, 0.26], [0, 0.015, 0]),
    // 잎 둘
    part(new BoxGeometry(0.20, 0.02, 0.10), [0.34, 0.52, 0.24], [0.10, 0.78, 0.02], [0, 0.5, 0.22]),
    part(new BoxGeometry(0.16, 0.02, 0.08), [0.30, 0.46, 0.22], [-0.07, 0.77, -0.06], [0, -0.8, -0.18]),
  ]),

  /**
   * 재떨이 (12cm) — 유리 재떨이.
   *
   * **가장자리 홈이 재떨이의 정체다.** 홈 없는 오목한 접시는 그릇이다.
   * 담배꽁초 둘이 마지막을 맡는다.
   */
  재떨이: () => assemble([
    // **진짜로 판다.** 예전엔 어두운 원기둥을 위에 얹어 파인 «척»만 했다
    ...hollow(0.50, 0.42, 0.22, 0.06, 0.07, 20, GLASS, [0.32, 0.40, 0.44]),
    // 담배 얹는 홈 둘 — 테두리를 가로지르는 막대
    part(new BoxGeometry(0.16, 0.05, 0.26), [0.42, 0.50, 0.55], [0.42, 0.215, 0]),
    part(new BoxGeometry(0.26, 0.05, 0.16), [0.42, 0.50, 0.55], [0, 0.215, -0.42]),
    // 꽁초 둘. 필터가 보여야 꽁초다
    part(new CylinderGeometry(0.038, 0.038, 0.26, 8), WRAP, [0.10, 0.235, 0.14], [0, 0.6, 1.5708]),
    // 꽁초 필터 — 담배 몸통과 «확실히» 갈려야 필터로 읽힌다(전 대비 0.06)
    part(new CylinderGeometry(0.042, 0.042, 0.09, 8), [1.35, 1.00, 0.42], [0.26, 0.235, 0.05], [0, 0.6, 1.5708]),
    // 재
    part(new SphereGeometry(0.07, 6, 4).scale(1, 0.4, 1), DARK, [-0.12, 0.16, -0.06]),
  ]),

  /**
   * 콘센트 (12cm) — **벽에 걸린다.**
   *
   * ## 왜 형상으로 옮겼나
   *
   * 여태 이건 `CityBuilding` 이었다. 그 엔진은 **2D 외곽선을 y=0 에서 위로 뽑는
   * 압출**이라 공중에 뜬 조각을 못 만든다(`City.geometryFor`). 그래서 콘센트가
   * **바닥에 선 12×16cm 흰 판**이었고, 구멍 대신 세운 어두운 막대 둘이
   * **바닥까지 내려왔다.** 사용자가 본 그림은 이랬다:
   *
   * > 「콘센트는 또 왜 저래.. **오니기리** 처럼 생겼잖아」
   *
   * 맞는 말이다. 바닥에 놓인 흰 덩어리에 검은 점 둘이면 그건 주먹밥이다.
   * 색을 고치거나 구멍을 예쁘게 판다고 될 일이 아니었다 — **벽에 걸려야 콘센트다.**
   * `StageProp.y` 가 이미 있어서(TV장 위의 텔레비전이 쓰는 길) 엔진은 안 건드린다.
   *
   * ## 무엇이 콘센트를 콘센트로 만드나
   *
   * 판이 아니라 **파인 원과 그 안의 구멍 둘**이다. 앞선 판본의 «세로 막대 둘»은
   * 흰 덩어리 위의 검은 점 둘이라 얼굴로 읽혔다. 원형 매입구는 그렇게 안 읽힌다.
   *
   * ## 자세
   *
   * 기본은 **+Z 를 보는 벽걸이**다 (`rotY` 로 다른 벽에 붙인다).
   * 최장축이 판의 가로세로(0.12)라 `normalize()` 뒤 두께는 0.019/0.12 ≈ 0.16 이다.
   */
  콘센트: () => assemble([
    // 판 — 벽에 붙는 얇은 사각 판. 모서리를 깎아야 «붙인 판»으로 보인다
    part(soft(0.12, 0.12, 0.012, 0.30), WHITE, [0, 0.06, 0.006]),
    /**
     * 매입구. **판 «앞»으로 솟은 고리여야 한다.**
     *
     * 처음엔 판을 통짜로 두고 그 «속»에 어두운 원반을 넣었다. 당연히 안 보였다 —
     * 판 앞면이 z=0.018 인데 원반이 z=0.005 에 있었으니 통째로 가려져 있었다.
     * 화면에서는 희미한 호 한 줄만 나왔고, 그건 여전히 「판에 점 둘」이다.
     * 판을 얇게 낮추고 고리를 «앞으로» 세워서 안이 들여다보이게 한다.
     */
    part(new CylinderGeometry(0.042, 0.042, 0.010, 18, 1, true), WHITE, [0, 0.06, 0.017], LIE_Z),
    // 안쪽벽 — 뒤집어야 «안»이 보인다. FrontSide 라 안 뒤집으면 통째로 컬링된다
    part(invert(new CylinderGeometry(0.036, 0.036, 0.010, 18, 1, true)),
      [0.58, 0.58, 0.58], [0, 0.06, 0.017], LIE_Z),
    // 고리 앞면 — 바깥벽과 안쪽벽을 잇는다. 없으면 테가 종이처럼 얇아 보인다.
    // **토러스는 이미 XY 평면(축이 Z)이라 돌리면 안 된다** — LIE_Z 를 먹였다가
    // 화면에서 가로 막대 한 줄로 나왔다
    part(new TorusGeometry(0.039, 0.003, 6, 20), WHITE, [0, 0.06, 0.022]),
    // 매입구 바닥. 어두워야 «파였다»가 보인다
    part(new CylinderGeometry(0.036, 0.036, 0.002, 18), [0.48, 0.48, 0.48], [0, 0.06, 0.013], LIE_Z),
    /**
     * 구멍 둘. **이게 콘센트의 정체다.**
     *
     * 진짜로 뚫지 않는다 — 바닥 원반에 구멍을 내려면 원반을 링으로 만들어야 하는데
     * 구멍이 한가운데 근처라 링으로는 안 된다. **밝은 바닥 위의 어두운 원반**이
     * 구멍으로 읽히고, 그게 저폴리에서 구멍을 그리는 방법이다.
     * 간격 0.0115(판 폭의 19%)는 실물(86mm 판에 19mm)과 같은 비율이다.
     */
    part(new CylinderGeometry(0.0065, 0.0065, 0.002, 10), DARK, [-0.0115, 0.06, 0.0145], LIE_Z),
    part(new CylinderGeometry(0.0065, 0.0065, 0.002, 10), DARK, [0.0115, 0.06, 0.0145], LIE_Z),
    // 고정 나사 둘. 판이 «벽에 박혀 있다»를 만든다
    part(new CylinderGeometry(0.004, 0.004, 0.004, 6), METAL, [0, 0.104, 0.013], LIE_Z),
    part(new CylinderGeometry(0.004, 0.004, 0.004, 6), METAL, [0, 0.016, 0.013], LIE_Z),
  ]),
};

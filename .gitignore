node_modules/
dist/

# OSM 재수집 전에 남겨둔 사본. 2.8MB 짜리라 이력에 넣을 이유가 없다.
*.bak

.DS_Store
*.log

# 의도 시트 산출물. 렌더는 언제든 다시 뽑을 수 있다.
.design-bounce/.tmp/
.design-bounce/sample/

# 레퍼런스 «사진» 자체는 남의 것이라 공개 저장소 이력에 넣지 않는다.
# 출처 URL 과 「본 것·잰 것」은 intent.md 에 남으므로 기록은 유지된다.
.design-bounce/ref/**/*.png
.design-bounce/ref/**/*.jpg
.design-bounce/ref/**/*.jpeg
.design-bounce/ref/**/*.webp
